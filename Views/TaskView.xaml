<Window x:Class="Tasks.TaskView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tasks"
        xmlns:viewModels="clr-namespace:Tasks.ViewModels"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        Title="Tasks" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:TaskViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Name:"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1"/>
                <Label Grid.Row="1" Grid.Column="0" Content="Description:"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1"/>
                <Label Grid.Row="2" Content="Priority:"/>
                <TextBox Text="{Binding Priority, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1"/>
            </Grid>
            <WrapPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal">
                <Button Command="{Binding AddTaskCommand}" Content="Add"/>
                <Button Command="{Binding RemoveTaskCommand}" Content="Remove"/>
                <Button Command="{Binding ClearInputsCommand}" Content="Clear"/>
                <Button Command="{Binding ModifyTaskCommand}" Content="Modify"/>
                <CheckBox IsChecked="{Binding ShouldClear}" Content="Clear on add"/>
                <CheckBox IsChecked="{Binding ShouldSelectNext}" Content="Should select next on remove"/>
            </WrapPanel>

        </Grid>
        <ListView SelectedItem="{Binding SelectedTask}" ItemsSource="{Binding Tasks}" Grid.Column="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox IsEnabled="false" Grid.Column="0" Text="{Binding Name}"/>
                        <TextBox IsEnabled="false" Grid.Column="1" Text="{Binding Description}"/>
                        <TextBox IsEnabled="false" Grid.Column="2" Text="{Binding Priority}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
